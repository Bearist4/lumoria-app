# Pass 2 — Data-at-rest findings (2026-04-20)

Spec: `docs/superpowers/specs/2026-04-20-security-audit-design.md`
Plan: `docs/superpowers/plans/2026-04-20-security-audit.md`

Severity scale: `crit` (fix inline, blocks pass) · `high` (fix inline) · `med` (defer to backlog) · `low` (defer) · `ok` (no issue found) · `unknown` (needs reviewer input).

| id | severity | location | description | action | commit |
|----|----------|----------|-------------|--------|--------|
| 2.2 | ok | Lumoria_AppApp.swift:45–56 · Item.swift | `ModelContainer` uses default `ModelConfiguration` (default URL in app sandbox, inherits `NSFileProtectionCompleteUntilFirstUserAuthentication`). Schema contains only the unused starter `Item` model — no ticket / memory / user data lives in SwiftData (those go to Supabase as AES-GCM ciphertext). | none | - |
| 2.3a | ok | LumoriaPKPassImport/ShareViewController.swift:134–165 · Lumoria_AppApp.swift:202–213 | App-group pkpass hand-off: share extension writes the pass to the container, main app drains + deletes on next foreground. File inherits default iOS protection (`UntilFirstUserAuthentication`). Share extension can fire while device is locked, so stricter `.complete` protection would break drop-from-lock flows. | none | - |
| 2.3b | low | ShareViewController.swift:145 | `data.write(to: target, options: .atomic)` does not explicitly set `.completeFileProtectionUntilFirstUserAuthentication` — relies on directory default. Adding the explicit option is defense-in-depth (cheap, no behavior change). | defer — backlog | - |
| 2.4 | ok | KeychainStore.swift · EncryptionService.swift | `KeychainStore` is used exclusively by `EncryptionService` for the per-user data-encryption key (scope confirmed — only two call sites). Crypto uses AES-GCM-256 (authenticated), 256-bit random key via `SymmetricKey(size: .bits256)`, per-call nonce via `AES.GCM.seal`, combined ciphertext carries nonce+tag. `kSecAttrSynchronizable: true` + `kSecAttrAccessibleAfterFirstUnlock` is deliberate (iCloud Keychain propagates the key so the same user can decrypt on a second device). | none | - |
| 2.5a | ok | PKPassImporter.swift:32–37 | `PKPass(data:)` runs Apple's native manifest signature check; malformed / untrusted archives throw `ImportError.unreadable`. | none | - |
| 2.5b | ok | PassJSONReader.swift:580–706 | Custom ZIP walker reads only `pass.json` bytes — no filesystem extraction, no path-based writes, so zip-slip is not reachable. JSON decoded with `JSONSerialization` and guard-cast to `[String: Any]`. Compression-bomb risk bounded by `uncompressedSize` header; capacity capped at `max(uncompSize, data.count * 4, 4_096)`. | none | - |
| 2.5c | ok | PKPassImporter.swift | `webServiceURL` / `authenticationToken` from `pass.json` are never read. Never actioned. No network call made as a side effect of parsing. | none | - |
| 2.5d | high | PKPassImporter.swift:40–107 | Four `NSLog` statements dumped ticket PII in **every build** (not just debug): full `pass.json` body, every field key/label/value, flight details (seat, flight number, origin/dest, gate, terminal), and train details (company, number, cities, car, seat). NSLog writes to the system log and crash reports. Real user PII egressed via on-device logs in release builds. | fixed inline — gated the four content logs behind `#if DEBUG`; structural / error logs kept (they don't leak content). | 97e8d7b |
| 2.6 | ok | Lumoria_AppApp.swift:178–186 · ImportStep.swift:101–112 | Both call sites for `startAccessingSecurityScopedResource()` use `defer { stop… }` with a balanced guard on the start return. No descriptor leak. | none | - |
| 2.7 | ok | StickerRenderService.swift · TicketsStore.swift | Error-path `print` logs include only row UUIDs + Swift error, never ticket content / email / venue / seat / barcode. (PKPass content-dump logs already addressed under 2.5d.) Grepped `email` / `venue` / `name` in `print(...)` → no matches outside already-addressed files. | none | - |
| 2.8a | ok | InviteView.swift:94,210 | Pasteboard copy only carries the public invite URL (`https://getlumoria.app/invite/{token}`) — the artifact is meant to be shared, so missing expiry / localOnly is by design. | none | - |
| 2.8b | low | Lumoria_AppApp.swift:114–121 scene-phase handler | No backgrounding snapshot obscuration — task-switcher previews show ticket lists. Defense-in-depth only; not a TestFlight blocker. | defer — backlog | - |
