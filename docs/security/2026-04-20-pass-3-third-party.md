# Pass 3 — 3rd-party / leak findings (2026-04-20)

Spec: `docs/superpowers/specs/2026-04-20-security-audit-design.md`
Plan: `docs/superpowers/plans/2026-04-20-security-audit.md`

Severity scale: `crit` (fix inline, blocks pass) · `high` (fix inline) · `med` (defer to backlog) · `low` (defer) · `ok` (no issue found) · `unknown` (needs reviewer input).

| id | severity | location | description | action | commit |
|----|----------|----------|-------------|--------|--------|
| 3.2 | ok | AnalyticsEvent.swift (all cases) | Every event's `properties` dict is either empty or carries enum `rawValue`s, counts, bools, `emailDomain` (e.g. "gmail.com" not full email), or explicit hashed IDs (`invite_token_hash`, `memory_id_hash`). No raw email, ticket name, venue, seat, barcode, photo URL or user-entered text is ever sent. `dataSyncFailed(reason:)` is declared but has no call sites. | none | - |
| 3.3 | ok | AnalyticsIdentity.swift · AmplitudeAnalyticsService.swift · AuthManager.swift:77–83 | Amplitude `userId` is the Supabase user UUID (opaque; not email). The only user property attached is `email_domain` (not the full address). IP tracking disabled via `TrackingOptions().disableTrackIpAddress()`. Autocapture limited to `.sessions` (no auto-tap / no screen capture). `amplitude.reset()` fires on sign-out (verified in Pass 1, 1.5). | none | - |
| 3.4a | ok | AmplitudeAnalyticsService.swift · Package.resolved | App links `AmplitudeUnified` (the meta-package) but `import AmplitudeSwift` is the only module imported. No `SessionReplayPlugin` instantiated, no `add(plugin:)` call, no `ATTrackingManager` usage. Session replay is not running. | none | - |
| 3.4b | med | Lumoria App.xcodeproj → Frameworks → AmplitudeUnified | `AmplitudeUnified` package transitively links `AmplitudeSessionReplay` even though the feature is never instantiated. Dead framework inflates bundle size and makes App Store review harder to justify (Apple sees the framework + its own tracking manifest). Switching the dependency to `amplitude-swift` directly would drop SessionReplay. | defer — backlog (pre-App-Store optimization, not TestFlight blocker) | - |
| 3.5 | ok | Info.plist:57–58 | Permissions actually used: push notifications (no purpose string required), `PhotosPicker` (permission-less by design on iOS 16+), `UIImageWriteToSavedPhotosAlbum` for gallery export (covered by the existing `NSPhotoLibraryAddUsageDescription`). No camera / microphone / contacts / calendar / FaceID / location / speech / motion APIs are used — `CoreLocation` appears only for `CLLocationCoordinate2D` math against a static airport database, no `CLLocationManager`. | none | - |
| 3.6 | high | Lumoria App/PrivacyInfo.xcprivacy (new) | App-level privacy manifest was missing (required by Apple). Created: declares collected data types (email, name, photos, user content → AppFunctionality; product interaction, device id → Analytics; crash data → AppFunctionality), declares `NSPrivacyAccessedAPICategoryUserDefaults` with reason `CA92.1`, `NSPrivacyTracking = false`, empty `NSPrivacyTrackingDomains`. | **fixed inline — file written**; `NSPrivacyAccessedAPICategoryFileTimestamp` etc. not needed (app doesn't read file timestamps). **Requires user: add `PrivacyInfo.xcprivacy` to the Lumoria App target in Xcode → Build Phases → Copy Bundle Resources** (Write does not update the Xcode project graph). | 8f33438 |
| 3.7 | high | Info.plist:52–53 (new key) | `ITSAppUsesNonExemptEncryption` was missing — every TestFlight upload would prompt for export-compliance self-classification. Lumoria's CryptoKit use is for user-authenticated data storage, which qualifies under the standard exemption (Category 5 Part 2 exempt), so declaring `false` skips the annual paperwork. | fixed inline — added `<key>ITSAppUsesNonExemptEncryption</key><false/>` to `Info.plist`. | 8f33438 |
| 3.8 | ok | Info.plist · all .plist files | No `NSAllowsArbitraryLoads`, no `NSAppTransportSecurity`, no `NSExceptionDomains` in any plist — ATS strict default is in effect. No `http://` URLs in app source (a single reference exists in `Lumoria_AppTests` purely to validate the deep-link handler rejects http scheme). | none | - |
| 3.9 | ok | LumoriaLinks.swift · Lumoria_AppApp.swift:172–233 · Invite.swift:117–147 | `LumoriaLinks.swift` is a static-constants brand file (no parsing). Deep-link handler allowlists `getlumoria.app` + `www.getlumoria.app` + the `lumoria://` scheme before any routing; auth callbacks require path prefix `/auth/`; pkpass import requires exact path `/import/pkpass`; invite parser requires `host == "invite"` or `/invite/{token}` path shape. No auto-action: invite tokens are stashed and only claimed server-side after the user is authenticated. | none | - |
| 3.10 | ok | Amplitude.xcconfig · Amplitude.sample.xcconfig · .gitignore | Real key lives in `Amplitude.xcconfig` (gitignored; `.gitignore` lists it explicitly). Sample file ships the placeholder `YOUR_AMPLITUDE_API_KEY_HERE`. `git log --all --follow -- Amplitude.xcconfig` returns empty — the real file has never been tracked. (Note: Amplitude's mobile "API key" is a client-embedded identifier, not a service secret — analogous to the Supabase anon key; keeping it out of git is hygiene rather than secret protection.) | none | - |
| 3.11 | ok | StoryRenderView.swift · export paths | Renders ticket exports from pure SwiftUI geometry via `ImageRenderer`; no `ImageIO` / `CGImageDestination` / EXIF-manipulation APIs anywhere in the codebase (grep clean). Exported `UIImage` bytes don't carry any embedded user-id or session metadata. | none | - |
| 3.12 | low | supabase/functions/send-push/index.ts:93–115 | Client-side push handling is clean (no PII in logs, no content surfaced beyond `alert.title` + `alert.body`). Server composer reads `notifications.title` and `notifications.message` directly into the APNs `alert` payload. Today those rows are populated by `announcements` broadcasts + onboarding content — no ticket content — but the schema permits PII. Policy: keep notification copy broadcast-level only, never include user-entered ticket names / venues / seats. | defer — backlog (product policy note, not a TestFlight blocker) | - |
| 3.13 | ok | LumoriaStickers/MessagesViewController.swift · LumoriaStickers/StickerManifest.swift · StickerRenderService.swift:189–202 | iMessage extension reads only the pre-rendered sticker PNGs + a small `manifest.json` (ticket UUID, filename, ISO timestamp, accessibility label). Accessibility label is `"<category> · <IATA> to <IATA>"` (airport/station codes, not PII). Extension never touches Supabase, Keychain, or the network. | none | - |
| 3.14 | ok | Package.resolved | Pinned versions: `supabase-swift@2.43.1`, `amplitude-swift@1.18.1`, `swift-crypto@4.3.1` (Apple). No publicly disclosed advisories on these specific versions as of 2026-04-20. Recommend a fresh advisory sweep (GitHub Dependabot / `npm audit` equivalent for Swift) immediately before first App Store submission. | none (TestFlight); re-sweep before App Store | - |
