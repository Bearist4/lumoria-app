# Pass 1 — Auth findings (2026-04-20)

Spec: `docs/superpowers/specs/2026-04-20-security-audit-design.md`
Plan: `docs/superpowers/plans/2026-04-20-security-audit.md`

Severity scale: `crit` (fix inline, blocks pass) · `high` (fix inline) · `med` (defer to backlog) · `low` (defer) · `ok` (no issue found) · `unknown` (needs reviewer input).

| id | severity | location | description | action | commit |
|----|----------|----------|-------------|--------|--------|
| 1.2 | ok | SupabaseManager.swift:11–18 | Supabase session uses SDK default (Keychain-backed on iOS); no custom `localStorage` override. Only UserDefaults use of a "token" is `PendingInviteTokenStore` storing a public invite code — not a credential. | none | - |
| 1.3 | ok | SupabaseManager.swift:13 | Only JWT in app bundle is the Supabase anon key (role=anon, confirmed by JWT payload). No `service_role` / `sb_secret` / `SUPABASE_SERVICE` tokens found in source or configs. | none | - |
| 1.4a | ok | PushNotificationService.swift:125,140 · TicketsStore.swift:81 | Logs contain the word "token"/"session" as a label only; printed values are Swift errors, never credentials. | none | - |
| 1.4b | low | InvitesStore.swift:165 | Release `print` includes the raw invite token value (`\(token)`). Token is a public Crockford base32 invite code, not a credential — but release logging of user-entered codes is unnecessary. | defer — backlog | - |
| 1.5 | ok | AuthManager.swift:66–70 · Lumoria_AppApp.swift:106–113 | Sign-out pipeline: Supabase clears its own Keychain session; `Analytics.reset()` fires on `.signedOut`; `pushService.signedOut()` fires on `isAuthenticated → false` (removes `device_tokens` row). Local SwiftData rows are retained but stay unreadable to other users (per-user AES-GCM key gates decryption). | none | - |
| 1.6 | high | ProfileView.swift:400–403 (was TODO stub) | "Delete my account" menu item was not implemented. App Store 5.1.1(v) requires in-app account deletion. | fix inline — deployed `delete-account` edge function (Supabase v2) that deletes every user-scoped row in FK order + avatar ciphertext + `auth.admin.deleteUser`; client wires confirmation alert → `supabase.functions.invoke("delete-account", body: {confirmation:"DELETE"})` → signOut. | a9c4791 |
| 1.7 | ok | Lumoria_AppApp.swift:172–233 · AuthRedirect.swift · ForgotPasswordView.swift:90–94 | Auth deep-link handler allowlists `getlumoria.app` + path prefix `/auth/` before calling `supabase.auth.session(from:)`. PKCE is the Supabase Swift SDK default (v2.43.1). Reset email form confirmation copy is non-enumerating ("If X has an account…"). | none | - |
| 1.8 | med | SignUpView.swift:271 | Sign-up error surfaces Supabase's raw `error.localizedDescription` — when an email is already registered, Supabase returns "User already registered", leaking existence of the account. Analytics already classifies the case as `.email_in_use`; UI copy does not. | defer — backlog | - |
| 1.9a | ok | supabase advisor — `public._push_debug` | `rls_enabled_no_policy` (INFO). Debug table has RLS on but no policies; server-side `send-push` writes via service_role (bypasses RLS), so default-deny is correct. | none | - |
| 1.9b | high | supabase advisor — `public.set_updated_at()` | `function_search_path_mutable` (WARN). Trigger function without pinned `search_path` is vulnerable to schema-based object hijacking. | fixed — migration `pin_trigger_function_search_paths` applied; advisor re-run confirms resolved. | migration |
| 1.9c | high | supabase advisor — `public.set_notification_prefs_updated_at()` | Same as 1.9b. | same migration; advisor confirms resolved. | migration |
| 1.9d | high | supabase advisor — auth: leaked password protection | `auth_leaked_password_protection` (WARN). HaveIBeenPwned check disabled — compromised passwords accepted. | **action required from user**: toggle on in Supabase dashboard → Auth → Policies → "Leaked password protection". Not something the MCP can flip. Record as outstanding until confirmed. | - |
