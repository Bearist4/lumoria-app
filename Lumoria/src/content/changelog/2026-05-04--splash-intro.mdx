export const meta = {
  version: '1.0.5',
  date: '2026-05-04',
  type: 'feature',
  title: 'Splash intro',
}

A 3-second cold-start intro now greets every launch. The four brand colours bounce in one after the other from the bottom — blue, orange, yellow, pink — the 7-point star reveals at centre and rotates a full turn, and the bars compress into the logomark at the exact slot the landing screen uses, before the wordmark, tagline and CTA fade in. The hand-off to the live root view is invisible: the splash's final frame is byte-identical to LandingView, so unauthenticated users see no jump.
